
FROM python:3.11-slim

WORKDIR /app

# Устанавливаем Python зависимости
RUN pip install fastapi uvicorn requests sqlite3

COPY admin_backend.py .
COPY data/ /app/data/

EXPOSE 8003

CMD ["python", "-m", "uvicorn", "admin_backend:app", "--host", "0.0.0.0", "--port", "8003"]
